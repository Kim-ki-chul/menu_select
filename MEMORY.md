# MEMORY.md

세션이 새로 시작되어도 이어서 작업할 수 있도록 진행 상황을 기록합니다.

## 확정된 결정 (변경 시 이 섹션도 갱신)

- 맛집 검색 = 카카오 로컬 API. 반경은 100m부터 시작해 5곳이 안 모이면 300/500/1000/2000m로 자동 확장 (`lib/kakao.js`).
- **별점/리뷰는 스크래핑하지 않는다.** 카카오 개발자 담당자가 데브톡(https://devtalk.kakao.com/t/topic/132083)에서 "오픈API 외 카카오 자체 서비스 데이터는 임의 사용 불가"라고 명시적으로 답변함. 대신 카카오 정확도순 정렬 + 네이버 블로그 검색 언급량을 보조 인기도 지표로 사용 (`lib/naver.js`).
- 정적 웹앱이 아니라 Node/Express 서버를 둔 이유 — 카카오/네이버/기상청 API가 전부 브라우저 CORS를 막고 있어서 서버 프록시가 필수.
- 날씨는 기상청 초단기실황(getUltraSrtNcst)의 PTY(강수)/T1H(기온)로 비/더움/추움/보통 4버킷 분류. `KMA_SERVICE_KEY` 없으면 "보통"으로 폴백.
- "전날과 중복 방지"는 달력상 정확히 어제가 아니라 "가장 최근 기록된 추천"과 다르게 처리 (앱을 매일 쓰지 않을 수도 있어서).
- 별점/리뷰수/사진 UI(사용자가 네이버 지도 스크린샷으로 요청)는 **구글 Places API(New)**로 구현. 네이버 지도 자체 별점도 카카오와 동일하게 오픈API 밖 데이터라 스크래핑하지 않기로 함. 구글 Places는 Google Cloud 결제 계정 필요(개인 사용량은 보통 월 $200 무료 크레딧 내), `places.rating`/`userRatingCount`/`photos` 필드는 Enterprise SKU 과금 대상. 사진은 `/api/photo`가 서버에서 대신 받아와 스트리밍 (구글 키를 프론트에 노출하지 않기 위함). 국내 소규모 로컬 맛집은 구글 커버리지가 카카오/네이버보다 부족할 수 있어 `topRestaurant.google`/`restaurant.google`이 `null`일 수 있음 (프론트에서 이미 처리됨).

## 현재 상태 (2026-09-03 기준)

- 전체 파일 구조 완성. `npm test` (menu.js 중복 제외 로직) 통과.
- **카카오 REST API 키는 실제 값이 `.env`에 들어가 있고 동작 검증 완료** — 제주 테스트 주소로 위치 저장 → 날씨 폴백("보통") → 메뉴 후보 → 반경 자동 확장(100m→2000m) → 식당 목록까지 end-to-end로 실제 호출 확인함. 테스트에 쓴 위치는 `data/store.json` 삭제로 초기화해둠.
- 네이버(`NAVER_CLIENT_ID/SECRET`), 기상청(`KMA_SERVICE_KEY`), 구글(`GOOGLE_MAPS_API_KEY`)은 아직 키가 없어 각각 폴백(언급량 0, 날씨 "보통", 별점/사진 null)으로만 확인됨.
- 사용자가 네이버 지도 검색결과 스크린샷(별점·리뷰수·사진)을 보여주며 같은 걸 요청 → 구글 Places API(New) 연동 추가함 (`lib/google.js`, `/api/photo` 프록시). 메뉴 후보 버튼 우측에 1위 식당 미리보기(사진+별점+리뷰수), 식당 5곳 카드에도 동일하게 표시하도록 프론트(`public/app.js`, `style.css`) 수정 완료. 구글 키가 없어 실제 사진/별점 렌더링은 아직 눈으로 못 봄 (null 폴백 동작만 확인).
- 검색 반경 UI는 native `<input type=range>` → 교통앱 스타일 채움 바 → 사용자가 그려준 스케치(점점 커지는 원이 선으로 연결된 모양)를 보고 최종적으로 **클릭형 버블 10개**(100~1000m, 100m 단위, 크기가 커짐, `.radius-bubble`, `#radius-bubbles`)로 정착. `public/app.js`의 `currentRadius` 변수가 상태를 들고 있고, 버블 클릭 시 `/api/recommend?radius=`와 (메뉴가 이미 선택돼 있다면) `/api/restaurants?radius=`를 같이 갱신.
- "다시 추천" 버튼(메뉴 후보 재추첨), 검색 반경 슬라이더(100~1000m, 100m 단위, `kakao.searchAtFixedRadius`) 추가. 처음엔 restaurant-section에 넣었다가, 사용자 요청으로 **menu-section("오늘의 메뉴 후보" 바로 아래)로 이동**해 메뉴 후보 미리보기(`/api/recommend?radius=`)와 식당 5곳 목록(`/api/restaurants?radius=`) 모두에 같은 반경이 적용되도록 변경. 슬라이더 조작 시 추천 메뉴 미리보기를 다시 불러오고, 이미 식당 목록을 보고 있었다면 그것도 같이 새로고침. 최대 반경은 사용자 요청으로 3000m→1000m로 축소. 슬라이더로 같은 메뉴를 반경만 바꿔 재검색할 때 `data/store.json` history에 중복 기록되지 않도록 `menu.recordChoice`를 당일·동일 메뉴면 스킵하도록 수정.
- "현 위치로 설정" 버튼 추가 — 브라우저 `navigator.geolocation`으로 좌표를 받아 `POST /api/location/current`로 전송, 서버는 카카오 좌표→주소 역변환(`kakao.reverseGeocode`, coord2address API)으로 사람이 읽을 주소를 만들어 기존 위치 저장과 동일하게 처리. 주소를 직접 타이핑하지 않아도 됨. 로컬(`localhost`)은 브라우저가 보안 컨텍스트로 취급해 HTTPS 없이도 Geolocation이 동작함.

## 다음에 할 일

1. 네이버 Client ID/Secret 발급받아 `.env`에 입력 (developers.naver.com).
2. 구글 Cloud 프로젝트 생성 + 결제 계정 등록 + Places API(New) 활성화 → `GOOGLE_MAPS_API_KEY` 발급 후 `.env`에 입력. (별점/리뷰/사진이 실제로 뜨는지는 이 키를 넣어야 확인 가능)
3. 모든 키를 넣은 뒤 사용자의 실제 주소로 브라우저에서 전체 플로우 재검증 (특히 메뉴 후보 카드 우측 미리보기, 식당 카드 사진/별점 표시).
4. 필요하면 `lib/menu.js`의 `MENU_TABLE` 메뉴 목록을 사용자 취향에 맞게 조정.

## 완료: "최근 선택한 메뉴" 리스트 복구 (2026-09-07)

- 제거 직후 사용자가 "바로 이전 버전으로 돌려줘"라고 요청 → git 저장소가 아니라서 직전에 지운 코드를 그대로 다시 타이핑해 복원함 (`#history-section`/`#history-list`, `GET /api/history`, `loadHistory()`, CSS 전부 원복). 결과적으로 지금은 있는 상태.
- **참고**: 이 프로젝트는 git init이 안 되어 있어서 "이전 버전으로" 요청은 매번 내가 직전 대화 내용을 기억해서 수동으로 되돌려야 함 — 되돌릴 일이 잦다면 git 저장소로 만드는 걸 사용자에게 제안해볼 것.

## 완료: "최근 선택한 메뉴" 리스트 제거 (2026-09-07)

- 추가한 지 얼마 안 돼서 바로 제거 요청받음. `#history-section`/`#history-list`(HTML), `GET /api/history`(server.js), `loadHistory()`(app.js), 관련 CSS를 전부 삭제.
- **주의**: `data/store.json`의 `history` 배열 자체와 그걸 쓰는 반복 방지 로직(`getRecentMenus`, `getLastMenu`, `recordChoice`)은 건드리지 않음 — 이건 "최근 선택 메뉴를 화면에 보여주는 기능"이 아니라 "같은 메뉴가 반복 추천되지 않게 하는 내부 로직"이라 완전히 별개 기능임. 사용자가 "history만 지워"라고 확인해준 것도 이 구분(보여주는 리스트 vs 내부 데이터/로직)과 일치.

## 완료: 메뉴 직접 검색 (2026-09-07)

- "오늘의 메뉴 후보" 카드 안, 후보 목록 아래에 검색창(`#menu-search-form`) 추가. 추천 알고리즘이 고른 후보가 마음에 안 들 때, 원하는 메뉴를 직접 타이핑해서 바로 식당 검색으로 넘어갈 수 있음.
- 새 백엔드 엔드포인트 없이 기존 `loadRestaurants()`/`GET /api/restaurants`를 그대로 재사용 (카카오는 어차피 임의 검색어를 받으므로). 검색으로 고른 메뉴도 똑같이 `history`에 기록됨.

## 완료: 웹 검색 기반 메뉴 풀 대규모 확장 (2026-09-03)

- "메뉴 풀을 웹에서 검색해서 최대한 늘려봐" 요청으로, hyugajung.com의 "직장인 점심 메뉴 추천 TOP 100" / "저녁 메뉴 추천 100가지" / "회사 회식 메뉴 추천 TOP 50" 글을 참고해 `CATEGORY_TABLES`를 재구성함.
- 날씨 버킷당 항목 수: 아침 7~9개, 점심 10~14개, 저녁 8~12개, 회식 9~14개 (기존 대비 대략 2~3배).
- 새로 추가된 항목은 전부 `MENU_CUISINE`에 태깅 완료 — `node -e`로 태그 누락 항목이 0개인지 확인함.
- 철자 통일: "돈카츠"는 기존 "돈까스"로 통일해서 사용 (근거 없이 비슷한 항목이 두 개로 나뉘는 것 방지).
- 기존 테스트가 의존하던 "점심/비 버킷엔 양식이 없다"는 전제는 그대로 유지함 (의도적으로 비 오는 날 점심에 양식 항목을 넣지 않아서 관련 폴백 테스트가 계속 유효함).

## 완료: 메뉴 풀 확장 + 반복 방지 알고리즘 업그레이드 (2026-09-03)

- 사용자가 "한식인데 백반도 없고 냉면만 계속 나온다", "검색 알고리즘 업그레이드가 필요하다"고 지적함. 원인 두 가지: (1) 날씨 버킷별 메뉴 풀이 너무 작았음(2~4개), 특히 한식 필터를 걸면 1~2개로 더 줄어듦. (2) 반복 방지 로직이 "가장 최근 1개"만 제외해서, 풀이 작으면 사실상 절반 확률로 계속 같은 메뉴가 나옴.
- **메뉴 풀 확장**: `CATEGORY_TABLES`의 각 날씨 버킷에 항목을 대폭 추가 (백반/제육볶음/된장찌개/삼계탕/콩국수/갈비탕/수제비 등). 특히 점심 카테고리는 버킷당 6~9개로 늘림.
- **반복 방지 알고리즘 업그레이드**: `getLastMenu`(직전 1개만 제외) 대신 `getRecentMenus(history, category, n)`로 **최근 3개**를 제외하도록 변경. 그래도 풀이 텅 비면 최근 1개만 제외 → 그래도 없으면 전체 풀, 단계적으로 완화 (`getCandidateMenus`).
- 실제 API로 4번 연속 조회해보니 삼계탕/냉면/비빔밥/콩국수/백반이 골고루 나오는 것 확인.

## 완료: 식당 없는 메뉴 후보 제외 (2026-09-03)

- `/api/recommend`가 이제 메뉴 후보 3개를 뽑을 때, 실제로 주변에 식당이 검색되는 메뉴만 후보로 채움. 카테고리+날씨+음식종류 조건의 전체 후보 풀(`getCandidateMenus(..., 99)`로 사실상 전체)을 무작위 순서로 훑으면서 식당이 있는 것만 최대 3개까지 채우고, 식당이 하나도 없는 메뉴는 조용히 건너뜀 (`server.js`).
- 후보가 0개면(=풀 안의 모든 메뉴가 반경 내 식당이 없음) 메뉴 후보 자리에 "현재 조건에 맞는 식당이 없습니다" 안내를 띄우고, 맛집 5곳 목록이 0개일 때도 같은 톤의 문구로 통일함 (`public/app.js`).
- 이 때문에 `topRestaurant`가 `null`인 후보가 프론트로 넘어올 일이 없어짐 (전엔 식당 없는 메뉴도 후보로 보여주면서 미리보기만 비워뒀었음).

## 완료: 아무거나/한식/중식/일식/양식 카테고리 (2026-09-03)

- 아침/점심/저녁/회식 카테고리와 검색 반경 사이에 음식 종류 버튼 5개 추가 (`#cuisine-section`). 기본값 "아무거나".
- `lib/menu.js`에 `MENU_CUISINE` 태그 맵을 추가해 기존 메뉴 문자열마다 한식/중식/일식/양식을 매겨둠 (수작업 태깅이라 애매한 것들 있음 — 예: 치킨/돈까스는 한식·일식 어느 쪽으로 봐도 되는데 임의로 정함, 마음에 안 들면 `lib/menu.js`의 `MENU_CUISINE`에서 바로 수정 가능).
- **업데이트 (같은 날 후속 수정)**: 사용자가 "회식+일식"을 골랐는데 치킨/삼겹살(둘 다 한식 태그)이 나와서 이유를 물어봄 → 확인해보니 회식 카테고리는 4개 날씨 버킷 전부 한식뿐이라 일식/중식/양식을 골라도 100% 폴백되고 있었음. `CATEGORY_TABLES.회식`의 각 날씨 버킷에 중식/일식/양식 옵션을 최소 1개씩 추가(마라탕/양꼬치/훠궈=중식, 이자카야 안주/스시 모둠/나베=일식, 크림파스타/스테이크/스튜=양식)하고, `menu.cuisineHasOptions()`로 폴백 여부를 서버 응답(`cuisineFallback`)에 실어 보내 프론트 추천 근거 문구에 "선택한 음식 종류가 없어서 전체에서 골랐다"고 안내하도록 함. 다만 나머지 조합(예: 아침+중식)은 여전히 콘텐츠를 다 채우지 않았고, 대신 안내 문구로 이유를 밝히는 쪽으로 처리함 — 모든 64개 조합(카테고리4×날씨4×음식종류4)을 다 채우진 않았다는 점 기억.
- 서버는 `/api/recommend`에 `cuisine` 쿼리 파라미터를 받고 응답에도 `cuisine`을 그대로 돌려줌. `/api/restaurants`는 이미 메뉴명이 확정된 뒤라 cuisine을 받지 않음.
- 제목도 "메뉴 결정장애 !!"로 변경함 (title 태그 + h1).

## 완료: 아침/점심/저녁/회식 카테고리 (2026-09-03)

- "오늘 날씨" 아래에 카테고리 버튼 4개 추가 (`#category-section`). 기본 선택은 "점심" (앱 원래 목적 유지).
- `lib/menu.js`의 `MENU_TABLE`을 `CATEGORY_TABLES = {아침, 점심, 저녁, 회식}`로 확장, 각 카테고리마다 4개 날씨 버킷별 메뉴 목록을 따로 둠 (내용은 임의로 채운 것이라 사용자 취향에 맞게 `lib/menu.js`에서 바로 수정 가능).
- `data/store.json`의 `history` 항목에 `category` 필드 추가. "직전과 다른 메뉴" 로직이 **카테고리별로 독립적으로** 동작하도록 함 — 예를 들어 오늘 점심에 냉면을 먹었어도 회식 메뉴 후보에서 냉면이 제외되지는 않음.
- `/api/recommend`, `/api/restaurants` 모두 `category` 쿼리 파라미터를 받음 (기본값 `점심`, 프론트는 항상 명시적으로 보냄). 카테고리 버튼을 누르면 그 카테고리+현재 반경으로 메뉴 후보를 다시 불러옴.
- 실제 키로 점심/저녁/회식 각각 다른 메뉴 풀이 나오는지, history에 category가 제대로 기록되는지 확인 완료.

## 완료: UI 정리 (2026-09-03)

- 카드 기반 레이아웃 + CSS 변수(`:root`)로 색상/간격/폰트 통일. 흰 카드+테두리+그림자를 `main > section`에 일괄 적용.
- "위치 변경" 버튼 삭제 — 사용자가 "재입력 후 저장하면 되는데 버튼이 왜 필요하냐"고 지적해서, 위치 폼을 항상 열어두는 쪽으로 단순화(`location-form`에서 `hidden` 제거, `change-location-btn` 관련 HTML/CSS/JS 전부 삭제). 저장된 주소가 있으면 폼 위/아래에 "현재 위치: ..."만 같이 보여줌.
- 날씨 표시 형식: "오늘 날씨 (기상청_단기예보) : 현재 {온도}도씨 / {버킷}" — `lib/weather.js`가 `temp`도 함께 반환하도록 이미 반영됨.

## 완료: 기상청 날씨 연동 (2026-09-03)

`KMA_SERVICE_KEY`를 `.env`에 입력함 — data.go.kr에서 받은 **Encoding(이미 URL 퍼센트인코딩된) 키**를 그대로 넣었음. `lib/weather.js`가 서비스키를 추가 인코딩 없이 URL에 바로 이어붙이는 방식이라 Encoding 키가 맞음 (Decoding 키를 넣으면 `+`/`=` 문자가 깨져서 요청이 실패함 — 다음에 키를 다시 받을 일이 있으면 이 점 기억할 것). 서울 강남 테스트 주소로 실제 호출해 `weatherConnected: true`, 버킷 "더움" 확인 완료.
