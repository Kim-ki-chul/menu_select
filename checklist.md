# 구현 체크리스트

- [x] 1. 프로젝트 스캐폴딩 (package.json, server.js, .env.example, .gitignore)
- [x] 2. 위치 저장 API + 프론트 입력폼
- [x] 3. 카카오 geocode/키워드검색 + 반경 확장 로직
- [x] 4. 기상청 날씨 연동 + 버킷 분류
- [x] 5. 메뉴 후보 테이블 + 중복 제외 로직 + test/menu.test.js
- [x] 6. 네이버 언급량 보조 정렬
- [x] 7. 프론트 전체 연결 (위치→날씨/메뉴→식당 5곳 카드)
- [x] 8. CLAUDE.md / MEMORY.md 최종 정리
- [x] 9. 카카오 실제 키로 end-to-end 검증 (위치→날씨폴백→메뉴→반경확장→식당목록)
- [x] 10. 구글 Places API 연동 (별점/리뷰수/사진) — 메뉴 후보 1위 식당 미리보기 + 식당 5곳 카드에 반영
- [x] 11. "현 위치로 설정" 버튼 — 브라우저 Geolocation + 카카오 좌표→주소 역변환으로 주소 직접 입력 없이 위치 저장
- [x] 12. "다시 추천" 버튼 — 메뉴 후보 재추첨
- [x] 13. 검색 반경 슬라이더 — 메뉴 후보 밑으로 이동, 100~1000m(100m 단위)로 메뉴 미리보기+식당 목록 전체에 적용, 히스토리 중복 기록 방지
- [x] 14. 반경 슬라이더를 교통앱 스타일 컬러 바로 리디자인 (채움 배경 + 바 안에 값 라벨) — 이후 15에서 버블 형태로 교체됨
- [x] 15. 반경 선택 UI를 사용자가 그려준 스케치(선으로 연결된, 점점 커지는 원)에 맞춰 100~1000m 10단계 버블로 재구현 — native range input 제거, 클릭형 버블 버튼으로 전환
- [x] 16. 반경 버블 크기 통일 + 선택값까지 누적 채색, 버블 사이 굵은 선(`#radius-line-fill`)도 같이 채워지도록 개선
- [x] 17. 기상청 API 키 적용 및 실제 날씨 연동 확인 (weatherConnected: true, 버킷 정상 분류)
- [x] 18. 날씨 표시에 실제 기온 추가 ("오늘 날씨: 30℃ / 더움") — `lib/weather.js`가 temp도 함께 반환하도록 수정
- [x] 19. 날씨 표시 문구 형식 변경 ("오늘 날씨 (기상청_단기예보) : 현재 30도씨 / 더움")
- [x] 20. UI 전면 리디자인 — 카드 레이아웃 + CSS 변수로 색상/간격/타이포 통일, 에러 메시지 알림 박스화
- [x] 21. "위치 변경" 버튼 제거 — 폼을 항상 열어두는 방식으로 단순화 (사용자 지적: 재입력해서 저장하면 되는데 버튼이 굳이 필요 없음)
- [x] 22. "오늘 날씨" 아래에 아침/점심/저녁/회식 카테고리 버튼 추가 — 카테고리별 메뉴 테이블(`CATEGORY_TABLES`)로 분리, history에 category 필드 추가해 카테고리별로 독립적으로 "직전과 다르게" 적용
- [x] 23. 검색 반경을 별도 카드(`#radius-section`)로 분리
- [x] 24. "오늘의 메뉴 후보" 아래에 추천 근거 문장 추가 (카테고리/날씨/직전 제외 메뉴) — `menu.getLastMenu` 추가해 서버가 `excludedMenu` 반환하도록 함
- [x] 25. 추천 근거 문장을 메뉴 후보 사이로 재배치, 참고 출처(기상청 단기예보/최근 추천 기록) 명시하도록 문구 보강
- [x] 26. 날씨 버킷별 이모지 추가 (비🌧️/더움☀️/추움❄️/보통⛅)
- [x] 27. 제목을 "메뉴 결정장애 !!"로 변경 (title, h1)
- [x] 28. 아침/점심/저녁/회식 카테고리와 반경 사이에 아무거나/한식/중식/일식/양식 음식 종류 카테고리 추가 — `MENU_CUISINE` 태그 + 날씨 버킷에 해당 종류가 없으면 전체 풀로 폴백
- [x] 29. 제목 중앙 정렬, 카테고리/음식종류 버튼별 색상 차등화, 카드 페이드인·버튼 pop 애니메이션 추가
- [x] 30. 회식 메뉴에 중식/일식/양식 실옵션 추가(전엔 전부 한식이라 항상 폴백됨), `cuisineFallback` 플래그로 폴백 시 이유를 화면에 안내
- [x] 31. 주변에 식당이 없는 메뉴는 "오늘의 메뉴 후보"에서 아예 제외 (topRestaurant 없는 후보 미노출), 후보가 0개면 조건에 맞는 식당이 없다는 안내 문구 표시. 5곳 목록도 같은 문구로 통일
- [x] 32. 메뉴 풀 대폭 확장(백반/제육볶음/삼계탕/갈비탕 등 추가) + "최근 3개와 안 겹치게" 반복 방지 알고리즘으로 업그레이드 (`getRecentMenus`)
- [x] 33. 웹 검색(직장인 점심/저녁/회식 메뉴 100선 등)으로 메뉴 풀을 대폭 확장 — 카테고리당 7~14개로 늘림, 전 항목 MENU_CUISINE 태깅 완료
- [x] 34. 하단에 "최근 선택한 메뉴" 섹션 추가 (`GET /api/history`, 최근 10개, 날짜·카테고리·메뉴)
- [x] 36. "오늘의 메뉴 후보" 아래에 메뉴 직접 검색 입력창 추가 — 기존 loadRestaurants 재사용, 추천 후보 없이도 원하는 메뉴로 바로 식당 검색 가능
- [x] 38. "최근 선택한 메뉴" 리스트 기능 완전 제거 (HTML 섹션, `/api/history`, `loadHistory`, 관련 CSS) — 반복 방지에 쓰는 내부 history 데이터/로직은 그대로 유지
- [x] 40. "최근 선택한 메뉴" 리스트 기능 복구 (바로 이전 버전으로 되돌림 요청)
- [x] 42. git 저장소 초기화 + 초기 커밋 (앞으로 "이전 버전으로" 요청 시 git으로 되돌릴 수 있음)
- [ ] 43. (사용자) 네이버/구글 키 발급 후 실제 키로 재검증 — MEMORY.md "다음에 할 일" 참고
